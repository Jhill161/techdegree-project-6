extends layout.pug
//- displays error in browser when user navigates to page that does not exist
block content
        h1= message
        h2= error.status
        p= error.stack

        a(href='/') ← Back